(function () {

    let glide = new Glide('.glide', {
        type: 'carousel',
        focusAt: 'center',
        perView: 1,
        gap: 20,
        slideWidth: 800
    })

    glide.mount()
})();
